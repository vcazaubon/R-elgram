// Dev runtime config. In production this file is regenerated by
// docker-entrypoint.sh (Spec 08) from the container environment.
// Values here are public (anon key) — never put secrets in env.js.
window.__ENV__ = {
  SUPABASE_URL: '',
  SUPABASE_ANON_KEY: '',
  API_URL: '/api',
  ALLOW_SIGNUPS: 'true',
};
